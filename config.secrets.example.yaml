# Helper: Rename this file to config.secrets.yaml and fill in your values
# DO NOT COMMIT THE ACTUAL config.secrets.yaml

ai:
  api_key: "YOUR_GROQ_API_KEY_HERE"

wireclub:
  email: "your_email@example.com"
  password: "your_password"

site_two:
  username: "your_username"

firebase:
  cred_path: "serviceAccountKey.json" # Ensure this file exists but is NOT committed
